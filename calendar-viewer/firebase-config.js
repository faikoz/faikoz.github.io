// DEMO: dummy config. The demo doesn't use Firebase — it loads a static
// calendar-data.json instead. This stub is left in place only so any
// leftover `import { firebaseConfig }` reference doesn't 404.
export const firebaseConfig = {
  apiKey: "demo",
  authDomain: "example.invalid",
  projectId: "demo",
  storageBucket: "example.invalid",
  messagingSenderId: "0",
  appId: "0:0:web:0",
};
